# Copy to .env and fill in. Only ever the anon (public) key — never service_role.
# Local dev: `supabase start` prints these (API URL + anon key).
# Hosted: Supabase Dashboard -> Project Settings -> API.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
